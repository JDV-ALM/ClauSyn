<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_commission" model="ir.ui.view">
        <field name="name">account.move.form.commission</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <!-- Agregar campos juntos después de invoice_date -->
            <xpath expr="//field[@name='invoice_date']" position="after">
                <field name="commission_reception_date"
                       string="Fecha de Recepción"
                       invisible="move_type not in ['out_invoice', 'out_refund', 'in_invoice', 'in_refund']"/>
                <field name="commission_seller_id"
                       string="Vendedor CS"
                       invisible="move_type not in ['out_invoice', 'out_refund']"
                       context="{'search_commission_seller': True}"
                       readonly="state != 'draft'"/>
            </xpath>
        </field>
    </record>
    
    <!-- Vista List -->
    <record id="view_invoice_tree_commission" model="ir.ui.view">
        <field name="name">account.move.list.commission</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_out_invoice_tree"/>
        <field name="arch" type="xml">
            <field name="invoice_user_id" position="after">
                <field name="commission_seller_id" optional="hide"/>
                <field name="commission_reception_date" optional="hide"/>
            </field>
        </field>
    </record>
    
    <!-- Filtros de búsqueda -->
    <record id="view_account_move_filter_commission" model="ir.ui.view">
        <field name="name">account.move.search.commission</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_account_move_filter"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="commission_seller_id"/>
            </field>
        </field>
    </record>
</odoo>